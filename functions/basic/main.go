package main

import "fmt"

func main() {

	x := 10
	y := 20
	// var s string

	x, y = intr(x, y)
	fmt.Println(x, y)
	x, y = intr(x, y)
	fmt.Println(x, y)
	x, y = intr(x, y)
	fmt.Println(x, y)
	hello()
}
